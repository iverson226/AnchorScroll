# 更新记录

## 发布与打包（1.0.3）

源码与文档以 Git 仓库形式发布，应用同时提供两种可分发产物：

* `AnchorScroll-1.0.3-macOS-arm64.dmg` —— 安装镜像，内含应用与「应用程序」快捷方式；
* `AnchorScroll-1.0.3-macOS-arm64.zip` —— 免安装压缩包。

两款产物均由仓库脚本生成：`build.sh` 负责内核测试、编译与 ad-hoc 签名，`make-dmg.sh` 负责生成并校验安装镜像。
校验值见 `release/SHA256SUMS.md`。同时选定 **MIT License** 作为项目开源许可证，并补充演示动画生成脚本
`scripts/make-demo.py`。

## 1.0.3

增加中键上下方向开关；保留 1.0.2 指针坐标修复。默认不反转，当前发布包包含应用图标。原生测试增加指针位置误差检查；GUI 验收未完成。

## 1.0.2

生成滚动事件时使用当前指针位置，避免 HID 固定锚点投递将实际指针拉回中心。

## 1.0.1

增加事件投递和手势阶段诊断参数。该版本存在真实指针回跳问题，不推荐使用。

## 1.0.0

首次实现独立菜单栏应用、速度曲线、小数余量累计、事件配对、设置和测试窗口。
